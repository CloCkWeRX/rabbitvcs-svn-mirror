diff -Nur -x '*.orig' -x '*~' trunk/setup.py trunk.new/setup.py
--- trunk/setup.py	2009-07-02 22:23:15.000000000 +0800
+++ trunk.new/setup.py	2009-07-02 22:25:09.000000000 +0800
@@ -124,10 +124,11 @@
 )]
 
 # Update notifier
-update_notifier = [("/usr/share/nautilussvn", [
-    "packages/ubuntu/debian/nautilussvn-restart-required.update-notifier",
-    "packages/ubuntu/debian/do-nautilussvn-restart-nautilus"
-])]
+# Not used for Debian package
+# update_notifier = [("/usr/share/nautilussvn", [
+#     "packages/ubuntu/debian/nautilussvn-restart-required.update-notifier",
+#     "packages/ubuntu/debian/do-nautilussvn-restart-nautilus"
+# ])]
 
 # Documentation
 documentation = [("/usr/share/doc/nautilussvn", [
@@ -170,7 +171,7 @@
             "ui/glade/*.glade"
         ]
     },
-    data_files=nautilus_extension + command_line_tool + translations + icons + documentation + update_notifier + config_spec
+    data_files=nautilus_extension + command_line_tool + translations + icons + documentation + config_spec # + update_notifier
 )
 
 #
@@ -178,10 +179,11 @@
 #
 
 # Make sure the icon cache is deleted and recreated
-if sys.argv[1] == "install":
-    print "Running gtk-update-icon-cache"
-    
-    subprocess.Popen(
-        ["gtk-update-icon-cache", icon_theme_directory], 
-        stdout=subprocess.PIPE
-    ).communicate()[0]
+# Debian: this is not needed since hicolor-icon-cache (0.10-2)
+# if sys.argv[1] == "install":
+#     print "Running gtk-update-icon-cache"
+#     
+#     subprocess.Popen(
+#         ["gtk-update-icon-cache", icon_theme_directory], 
+#         stdout=subprocess.PIPE
+#     ).communicate()[0]
